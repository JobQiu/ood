HW07: RMI

INSTRUCTIONS
1. To start the Compute Engine client, run jkl1.client.controller.ClientController.
2. Connect to a Compute Engine server by entering its IP address into the
   “Remote Host” field and clicking “Connect”.
3. The following tasks are available to send to the compute server. Status
   messages and computation results (where applicable) are displayed in the
   lower text pane.

   Get Info: get information about the server runtime environment.
   Calculate Pi: calculate pi to the number of digits supplied in Parameters.
   To Lowercase: convert the string supplied in Parameters to lowercase.
   Get Server Time: display the server’s current local time.
   Chat: open a new window for two-way text chat to the server.