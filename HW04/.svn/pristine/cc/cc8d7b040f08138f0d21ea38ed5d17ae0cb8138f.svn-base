package strategy;

import model.ABall;

/**
 * A IUpdateStrategy implementation that does not update the state of a ball.
 *
 */
public class StraightStrategy implements IUpdateStrategy {

	@Override
	public void updateState(ABall context) {
	}

}
