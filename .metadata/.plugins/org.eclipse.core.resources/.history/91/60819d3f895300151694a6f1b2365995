package util;

import java.util.Observable;

public class Dispatcher extends Observable {
    
    public void notifyAll(Object observer) {
        setChanged();
        notifyObservers(observer);
    }
}
