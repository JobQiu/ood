package model.ball;

import java.awt.Color;
import java.awt.Component;
import java.awt.Point;

public class CurveBall extends ABall {
    
    private double theta = 2 * Math.PI / 360;

    public CurveBall(Point startLoc, int startRadius, Point startVel, Color color, Component canvas) {
        super(startLoc, startRadius, startVel, color, canvas);
    }

    @Override
    protected void updateUniqueProperty() {
        velocity.x = (int)Math.round((Math.cos(theta) * velocity.x - Math.sin(theta) * velocity.y));
        velocity.y = (int)Math.round(Math.cos(theta) * velocity.y + Math.sin(theta) * velocity.x);
    }
}
